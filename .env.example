# Stellar Remit — Orange Belt environment
# Copy to .env.local and fill in the deployed contract addresses.
# Both are produced by the deployment sequence documented in README.md.

VITE_CROWDFUND_ID=
VITE_SPLITTER_ID=

# Optional overrides (defaults target Stellar testnet)
# VITE_RPC_URL=https://soroban-testnet.stellar.org
# VITE_HORIZON_URL=https://horizon-testnet.stellar.org
