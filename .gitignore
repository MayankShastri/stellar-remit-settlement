node_modules/
dist/

# Rust / Soroban build artifacts
target/
contracts/*/target/
contracts/*/test_snapshots/

# Environment — real contract IDs live in .env.local (see .env.example)
.env
.env.local
.env.*.local

# Stellar CLI local state (keys, aliases)
.stellar/

# OS / editor
.DS_Store
Thumbs.db
*.local

# Local design drafts — not part of the shipped project
HTML REFERENCES/

# Local AI-assistant tooling (graft init artifacts) — personal, not project
.claude/
.mcp.json
AGENTS.md
opencode.json

# graft's local graph cache — regenerable, not committed (run `graft build`).
graft/
